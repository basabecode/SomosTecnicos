'use client'

export function TermsEnforcer({ children }: { children: React.ReactNode }) {
  // Suspendido temporalmente: Renderizamos los hijos directamente sin verificación
  return <>{children}</>
}
